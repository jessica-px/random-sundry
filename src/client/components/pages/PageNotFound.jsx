import React from 'react';
import NavBar from './../NavBar.jsx';

const PageNotFound = (props) => (
    <div>
        <div className='pageTitle'>Oops! We can't find this page.</div>
        This is a 404 page, where all broken links redirect to.
    </div>
)

export default PageNotFound;